#include "main.h"
/**
  *main - entry point of the program
  *Description: Takes other functions and puts:
  *them together to display text
  *
  *Return: always returns a 0
  */

int main(void)
{
	char word[50] = "_putchar.";

	print_text(word);
	return (0);
}
