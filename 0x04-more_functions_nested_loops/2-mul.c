/**
 * mul - evaluates the product of 2 integers
 * @a: first integer
 * @b: second integer
 * Return: product of the 2 integera
 */

int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
