#include "main.h"

/**
 * main - entry point
 * Return: Always 0
 */

int main(void)
{
	char *str1, *str2, *str3;

	str1 = "My first strlen!";
	str2 = "";
	str3 = "Holberton!";

	_puts(str1);
	_puts(str2);
	_puts(str3);
	return (0);
}
