#include <stdio.h>
/**
 * main - entry point of the program
 * Description: what this function does is that
 * it displays a statement using the puts method
 * Return: 0 is what is returned from main
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
